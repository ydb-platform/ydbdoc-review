---
type: implementation-question
status: open
tags: [task-51797, remediation, policy-gate]
---

The required whole-worktree policy gate cannot be implemented to GREEN under the
current normative rules. At executor start, the delta against
`9ff8edec9a26d3975306e20adca325c6eb9f77e6` already contains 85 tracked paths
and 29 untracked paths, including `src/ydbdoc_review/cli.py`,
`src/ydbdoc_review/github/git_ops.py`, and the removed
`src/ydbdoc_review/harness/**` tree. Those paths are outside the union of
`allowed_files` in `implementation-plan.yaml`, while `paths_outside_allowlist`
is unconditionally RED and the manifest scope is explicitly the entire final
worktree delta.

Please amend the plan to state whether those pre-existing paths are an allowed
baseline inventory exception, or extend the allowlist and requirement mapping.
Without that, the mandated gate must fail before functional testing.
