---
protocol_version: 1
task_id: TASK-51797-ONE-PASS-REMEDIATION
review_type: specification_reviewer
specification_version: "one-pass-remediation-v023"
status: completed
verdict: CHANGES_REQUESTED
reviewer: specification_reviewer
has_unresolved_developer_choices: true
implementation_authorized: false
---

# Review response: factual Ruff baseline correction v023

## Verdict

**CHANGES_REQUESTED**

## Summary

Independent reproduction confirms the core diagnostic arithmetic and several
supporting facts: Ruff `0.16.5`, identical base/current `pyproject.toml` SHA-256
`822a7d65…ec29`, 265 base Python paths, 110 delta Python paths (56/19/35), 174
base-untouched paths with exactly 198 archive and current diagnostics, 108
diagnostics on historical present delta paths (including the stated 8 on three
new files), and total current diagnostics 306. Schema v2 partitioning,
git-only enumeration, absolute executable / relative metadata, and fail-closed
multiset rules are otherwise sound and do not introduce a waiver.

The frozen/active path accounting is not factually closed.

## Findings

- id: REMEDIATION-V023-001
  severity: RED
  related: [R-004, v020]
  summary: >
    `frozen_current_paths: 70` and “five active paths remain zero” contradict
    the measured present-delta set.
  detail: >
    Present delta Python files are 75 (= 56 modified + 19 new). Only four of the
    five named active paths currently exist and are in that set
    (`navigation_merge.py`, both remediation gate scripts, and
    `test_remediation_policy_gate.py`); those four are Ruff-clean.
    `tests/unit/test_remediation_ruff_gate.py` is absent (not in the delta), so
    linting it yields E902 and it cannot be counted as an active present path
    with zero diagnostics. Present-delta minus the four existing active paths
    is 71 frozen paths with the measured 108 diagnostics. After the missing
    test file is created, present delta becomes 76 and frozen remains 71
    (76 − 5). The plan’s `75 − 5 = 70` subtracts a non-existent active path.
    Correct the exact frozen path count (and any dependent capture assertions)
    and state active-zero as a post-creation requirement for the fifth path,
    not as a current reproduced fact for all five.

## Checks performed

- Claimed `review-request-v023.md`; read v023 amendments and current
  `scripts/remediation_ruff_gate.py` presence.
- Reproduced path counts, 198/108/306 diagnostics, config hash and Ruff version
  with git-only enumeration and resolved `.venv/bin/ruff`.
- Confirmed four existing active paths are clean; fifth absent; frozen = 71.
