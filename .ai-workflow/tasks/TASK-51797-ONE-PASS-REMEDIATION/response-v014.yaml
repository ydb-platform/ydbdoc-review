---
protocol_version: 1
task_id: TASK-51797-ONE-PASS-REMEDIATION
review_type: specification_reviewer
specification_version: "one-pass-remediation-v014"
status: completed
verdict: APPROVED
reviewer: specification_reviewer
has_unresolved_developer_choices: false
implementation_authorized: false
---

# Review response: approved-contract legacy tests v014

## Verdict

**APPROVED**

## Summary

v014 correctly aligns five legacy expectations to already approved contracts
without production, fixture or unrelated test changes.

1. **R-006 / chunker**: both `test_chunker_on_real_fixtures` and
   `test_chunker_reasonable_batch_count_on_real_files` currently parse every
   fixture as valid. Propagating the exact v010 path
   `en/core/reference/ydb-sdk/topic.md` with `AmbiguousYfmStructureError` /
   `unowned direct-depth`, exact-set guard, and continue-without-chunking
   matches the APPROVED v010/v013 pattern.
2. **R-015 / image alt**: current test still expects obsolete `⟦S1⟧` wrappers;
   replacement with exact `⟦IMGBEGIN_1⟧…⟦IMGEND_1⟧` source and translated shapes,
   `⟦S1⟧` absent, and preserved render/src assertions is closed and matches the
   approved image-atom contract.
3. **R-012 / orchestrator**: current assertion on `protect-token sequence` is
   obsolete; required substring
   `translation_acquisition_exhausted: role=translate` matches live
   `acquisition.py` diagnostics; count assertions stay unchanged.
4. **Environment**: three Eliza TLS `PermissionError` cache writes are
   classified environment-only with writable `XDG_CACHE_HOME` rerun; no product
   or test semantic edit authorized.

Resolver requires exact predecessor `one-pass-remediation-v013` and self-seals
four v014 protocol paths only. Forbidden list blocks wildcards, skip/xfail,
broad exceptions, production/fixture edits and weakening contracts. No developer
choice remains.

## Findings

[]

## Checks performed

- Claimed `review-request-v014.md`; read v014 amendments and spot-checked worktree
  tests for the three obsolete expectations.
- Confirmed R-006 path/exception match APPROVED v010.
- Confirmed `translation_acquisition_exhausted` string in worktree acquisition.
- Verified allowed paths/symbols and v013 predecessor / four-path self-seal.
