---
protocol_version: 1
task_id: TASK-51797-ONE-PASS-REMEDIATION
review_type: specification_reviewer
specification_version: "one-pass-remediation-v020"
status: completed
verdict: CHANGES_REQUESTED
reviewer: specification_reviewer
has_unresolved_developer_choices: true
implementation_authorized: false
---

# Review response: fail-closed Ruff baseline v020

## Verdict

**CHANGES_REQUESTED**

## Summary

The Ruff gate design itself matches R-004's required baseline-aware command:
capture-once from `git archive` of base `9ff8edec…`, exact 306 untouched
diagnostics with closed record schema, every present changed `*.py` must be
Ruff-clean, validation is multiset-subset fail-closed (not count-only), and
`noqa`/config/waiver escapes are forbidden. Predecessor `one-pass-remediation-v019`
and four-path protocol self-seal are exact.

However the amendment does not close how the three new Ruff artifacts enter the
resolved policy-gate allowlist and manifest mapping. Without that, refresh/
validate cannot classify them deterministically.

## Findings

- id: REMEDIATION-V020-001
  severity: RED
  related: [R-004]
  summary: >
    Missing closed R-004/item allowlist and manifest mapping for the three Ruff
    artifacts.
  detail: >
    `allowed_changes` lists `scripts/remediation_ruff_gate.py`,
    `tests/unit/test_remediation_ruff_gate.py` and
    `.ai-workflow/tasks/TASK-51797-ONE-PASS/ruff-baseline-v020.json`, and the
    gate tests mention "three_Ruff_artifact_paths", but v020 does not extend
    R-004 `allowed_files` / `allowed_symbols` (currently only the policy gate,
    report, snapshot and manifest) nor define exact
    ownership_class / requirement_ids / baseline_state / mapping_source for each
    path. The generated baseline JSON also needs an explicit capture-once /
    absent-to-present control-artifact rule analogous to other generated
    remediation artifacts. Close those exact fields so the implementor cannot
    invent mapping or leave the paths unmapped at refresh.

## Checks performed

- Claimed `review-request-v020.md`; read v020 amendments against R-004 and v019.
- Verified fail-closed Ruff capture/validate contract and schema are otherwise
  closed.
- Confirmed current R-004 `allowed_files` omit the three new Ruff paths.
- Confirmed `post_capture_control_paths_addition` seals only the four protocol
  files.
