CREATE TABLE run_objects (
    run_id Utf8, object_key Utf8, generation Utf8, part_no Uint32,
    created_at Timestamp, payload String,
    PRIMARY KEY (run_id, object_key, generation, part_no)
) WITH (TTL = Interval("P14D") ON created_at);
