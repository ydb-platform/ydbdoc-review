# Статус перехода на структурный перевод

## Текущее состояние

- Контракт: `a128c38160e5322296f2358aae4b63ae0b9b51a4`; независимая проверка Astra и узкие исправления: `ceeb7bb7e78421ccdd899e988c196354a63fb185`.
- Выполнено: требования, оба банка знаний и промпт диспетчера обновлены. Удалены два доказанно отменённых marker-only набора тестов: `4408e5152709ce23496cf2d1170f147a93599b6a`.
- Проверенная выборка соседних тестов: 132 passed, 10 deselected, 2.24 s, самый медленный 0.21 s. Полный suite RED на collection за 1.53 s: `tests/contract/test_report_f10.py` импортирует удалённый `initial_description`.
- Около 25 legacy `pytest.mark.timeout(120)` обнаружены диагностическим запуском. Ни один тест дольше минуты не подтверждён. Полный прогон был остановлен на 17%, так как после collection-ошибки не даёт достоверного timing summary.
- Две архитектурные попытки остановлены watchdog после 15 и 10 минут соответственно до сохранения плана. Следующий шаг: коротко восстановить план задач из требований и проверенных путей, затем запустить нового разработчика на первую задачу: устранить collection blocker и адаптировать устаревшие тесты.

## Правила восстановления

1. Сначала прочитать этот файл, `REQUIREMENTS_RU.md` и `docs/translation-structure-dispatch-prompt.md` из удалённого `main`.
2. Проверить SHA `main`, активных агентов и их последний результат.
3. Не создавать worktree. Любой принятый этап фиксировать отдельным commit в `main`.
4. В каждой задаче: новый разработчик, затем новый независимый тестер. Задача дольше 15 минут требует расследования, а не молчаливого ожидания.
